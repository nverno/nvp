# -*- mode: snippet -*-
# name: binary-search
# key: n_bs
# --
int l = 0, r = ${1:hi};
while (l != r) {
    int mid = (l + r + 1) / 2;
    if (${2:check(mid)}) l = mid;
    else r = mid - 1;
}
return l;