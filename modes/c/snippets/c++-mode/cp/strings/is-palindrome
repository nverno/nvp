# -*- mode: snippet -*-
# name: check palindrome
# key: s_ispalindrome
# --
bool is_palindrome(string const& s) {
    int l = 0, r = s.size() - 1;
    while (l < r) if (s[l++] != s[r--]) return false;
    return true;
}
