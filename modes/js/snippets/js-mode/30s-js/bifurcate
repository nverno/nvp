# -*- snippet -*-
# name: bifurcate
# key: f_bifurcate
# contributor: 30-seconds
# --
const bifurcate = (arr, filter) =>
  arr.reduce((acc, val, i) => (acc[filter[i] ? 0 : 1].push(val), acc), [[], []]);