# -*- mode: snippet -*-
# name: bfs
# key: bfs
# --
let q = [${1:0}];${2:
let l = 0}
while (q.length) {
  let sz = q.length;
  while (sz--) {
    let ${3:x} = q.shift();
    $0
  }${2:$(and (yas-text) "
  l++;")}
}
