# -*- mode: snippet -*-
# name: bfs
# key: bfs
# --
let q = [${1:0}];${2:
let ${3:l} = ${4:0};}
while (q.length) {
  let sz = q.length;
  while (sz--) {
    let ${5:x} = q.shift();
    $0
  }${2:$($s? (concat "\n" (yas-field-value 3) "++;"))}
}
