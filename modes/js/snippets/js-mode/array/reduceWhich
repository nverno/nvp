# -*- mode: snippet -*-
# name: reduceWhich
# key: f_reduceWhich
# contributor: 30-seconds
# --
const reduceWhich = (arr, comparator = (a, b) => a - b) =>
  arr.reduce((a, b) => (comparator(a, b) >= 0 ? b : a));