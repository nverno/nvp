# -*- mode: snippet -*-
# name: for
# key: for
# --
for (${1:let ${2:i} = ${3:0}};${4:$(if (> (length yas-text) 0) " " "")}${4:$2 < ${6:a}.length};${7:$(if (> (length yas-text) 0) " " "")}${7:$2++}) {
    $0
}