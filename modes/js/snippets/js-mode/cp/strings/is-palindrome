# -*- mode: snippet -*-
# name: is palindrome
# key: s_is_pali
# --
const is_pali = (s: string) => {
  let i = 0, j = s.length - 1;
  while (i < j) if (s[i++] != s[j--]) return false;
  return true;
};
