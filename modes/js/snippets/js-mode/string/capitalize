# -*- mode: snippet -*-
# name: capitalize
# key: f_capitalize
# contributor: 30-seconds
# --
const capitalize = ([first, ...rest]: any, lowerRest = false) =>
  first.toUpperCase() + (lowerRest ? rest.join('').toLowerCase() : rest.join(''));