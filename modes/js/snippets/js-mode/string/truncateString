# -*- mode: snippet -*-
# name: truncateString
# key: f_truncateString
# contributor: 30-seconds
# --
const truncateString = (str, num) =>
  str.length > num ? str.slice(0, num > 3 ? num - 3 : num) + '...' : str;