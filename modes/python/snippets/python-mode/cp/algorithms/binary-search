# -*- mode: snippet -*-
# name: binary search
# key: bs
# --
def check(m):
    $0

l, r, res = ${1:1}, ${2:1e9}, ${3:-1}
while l <= r:
    mid = (l + r) // 2
    if check(mid):
        res = mid
        r = mid - 1
    else:
        l = mid + 1
