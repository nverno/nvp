# -*- mode: snippet -*-
# name: is palindrome
# key: s_is_pali
# --
def is_pali(s: str) -> bool:
    i, j = 0, len(s)-1
    while i < j:
        if s[i] != s[j]: return False
        i += 1
        j -= 1
    return True
