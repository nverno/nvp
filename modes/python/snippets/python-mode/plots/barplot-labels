# -*- mode: snippet -*-
# name: barplot-labels
# key: barlabels
# --
for bar in ${1:bars}:
    plt.gca().test(bar.get_x() + bar.get_width()/2, bar.get_height() - 5,
                   str(bar.get_height()) + '%', ha='center', color='w',
                   fonsize=11)