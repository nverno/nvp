# -*- mode: snippet -*-
# name: build
# key: build
# --
# shellcheck source=/dev/null

set -eo pipefail

DIR="\$( cd "\$( dirname "\${BASH_SOURCE[0]}" )" && pwd)"
. "$DIR/../vars.sh"
. "$INCDIR/build-utils.sh"

$0
_process_args ${1:`(nvp-yas-bfn-no-ext)`}_install $1_${2:update} $1_${3:uninstall} "\$@"